import { mockChatProvider } from "./mock";

// Swap this export when connecting to Langflow in Phase 2:
// export { langflowChatProvider as chatProvider } from "./langflow";
export const chatProvider = mockChatProvider;
